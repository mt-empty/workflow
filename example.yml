name: check file exists
description: check if file exists
events: 
  fileExists:
    path: ./check_file_exists.sh
      success: 
        action:
          path: ./print.sh
        failure:
          - echo "file does not exist"

